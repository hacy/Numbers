spring:
  application:
    name: assignment
  data:
    mongodb:
      authentication-database: admin
      username: root
      password: root
      database: numberdb
      port: 27017
      host: localhost
server:
  port: 8102
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true



